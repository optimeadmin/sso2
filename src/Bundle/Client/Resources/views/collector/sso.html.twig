{% extends '@WebProfiler/Profiler/layout.html.twig' %}

{# {% block toolbar %} #}
{#    {{ parent() }} #}

{#    {% if collector.firewall and not collector.token %} #}
{#        {% set url = sso_local_login_start_url() %} #}
{#        {% if url is not empty %} #}
{#            <div class="sf-toolbar-block sf-toolbar-block-sso-login sf-toolbar-status-normal" {{ block_attrs|default('')|raw }}> #}
{#                <div class="sf-toolbar-icon"> #}
{#                    <div class="sf-toolbar-value">SSO</div> #}
{#                </div> #}
{#                <div class="sf-toolbar-info"> #}
{#                    <div class="sf-toolbar-info-piece" style="font-size: 14px"> #}
{#                        <a href="{{ url }}">Start local login SSO</a> #}
{#                    </div> #}
{#                </div> #}
{#            </div> #}
{#        {% endif %} #}
{#    {% endif %} #}
{# {% endblock %} #}

{% block toolbar %}
    {% set icon %}
        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none"
             stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"
             class="icon icon-tabler icons-tabler-outline icon-tabler-shield-lock">
            <path stroke="none" d="M0 0h24v24H0z" fill="none"/>
            <path d="M12 3a12 12 0 0 0 8.5 3a12 12 0 0 1 -8.5 15a12 12 0 0 1 -8.5 -15a12 12 0 0 0 8.5 -3"/>
            <path d="M12 11m-1 0a1 1 0 1 0 2 0a1 1 0 1 0 -2 0"/>
            <path d="M12 12l0 2.5"/>
        </svg>
        <span class="sf-toolbar-value">SSO</span>
    {% endset %}

    {% set text %}
        {# this is the content displayed when hovering the mouse over
           the toolbar panel #}
            <div class="sf-toolbar-info-piece">
                <span><a href="{{ collector.url }}">Start local login SSO</a></span>
            </div>
    {% endset %}

    {# the 'link' value set to 'false' means that this panel doesn't
       show a section in the web profiler #}
    {% if collector.show %}
        {{ include('@WebProfiler/Profiler/toolbar_item.html.twig', { link: false }) }}
    {% endif %}
{% endblock %}